You are writing the EXECUTIVE SUMMARY for an automated console testing run.

You are NOT deciding the verdict. The verdict is already fixed and must not be
changed, softened, or contradicted.

# Fixed verdict

{{ verdict }}

# Requirement context

{{ requirement | tojson }}

# Scenario

{{ scenario | tojson }}

# Health

{{ health | tojson }}

# Execution

{{ execution | tojson }}

# Verification

{{ verification | tojson }}

# RCA

{{ rca | tojson }}

# Rules

1. **Do not override the verdict.** `verdict_statement` must be consistent
   with the supplied verdict and verification summary.
2. **Be evidence-backed.** `strongest_evidence` should mention the most
   important observed fact, not speculate.
3. **Distinguish rig fault vs product defect clearly.**
4. **Keep it concise and human-readable.**
5. **Recommended next action must be concrete.**

# Output

Return an `ExecutiveSummary` with:
- `what_was_requested`
- `what_was_attempted`
- `verdict_statement`
- `strongest_evidence`
- `rca_summary`
- `recommended_next_action`
