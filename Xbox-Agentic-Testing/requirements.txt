# ---------------------------------------------------------------------------
# Xbox-Agentic-Testing - multi-agent console test runner
#
# Install:  pip install -r requirements.txt
#
# The LLM provider packages are all optional; install only the one you use.
# The framework picks the provider dynamically from config/env, so nothing
# below is hard-wired into the code.
# ---------------------------------------------------------------------------

# --- core orchestration ----------------------------------------------------
langgraph>=0.2.40
langchain>=0.3.0
langchain-core>=0.3.0

# --- LLM providers (install at least one) ----------------------------------
langchain-anthropic>=0.3.0
# langchain-openai>=0.2.0
# langchain-google-genai>=2.0.0
# langchain-ollama>=0.2.0

# --- data / config ---------------------------------------------------------
pydantic>=2.7
PyYAML>=6.0
python-dotenv>=1.0

# --- perception (shared with Xbox-Automation-Python) -----------------------
opencv-python>=4.9
numpy>=1.26

# --- reporting -------------------------------------------------------------
jinja2>=3.1

# --- optional OCR (vision tools degrade gracefully without these) ----------
# pytesseract>=0.3.10
# paddleocr>=2.7
